node_modules
dist

# Environment files (never commit secrets)
.env
.env.*
!.env.example

# Build / OS / editor noise
.DS_Store
*.log
proto.log
nohup.out

# User-uploaded images (written at runtime, not source)
packages/server/uploads/

# Local Claude / dev tooling
.claude/
.playwright-cli/
.playwright-mcp/
outputs/
